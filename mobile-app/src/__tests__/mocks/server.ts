/**
 * Mock Server Setup
 *
 * MSW (Mock Service Worker) setup for API mocking in tests.
 */

// Placeholder for MSW server setup
// This would typically use setupServer from 'msw/node'

export const server = {
  listen: () => {},
  resetHandlers: () => {},
  close: () => {},
  use: (..._handlers: unknown[]) => {},
};

export const handlers: unknown[] = [];
