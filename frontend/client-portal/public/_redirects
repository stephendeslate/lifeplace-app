# Cloudflare Pages SPA fallback is handled by 200.html (copied from __spa-fallback.html in build step).
# Pre-rendered routes (/, /about, etc.) have their own index.html and are served directly.
# No redirect rules needed.
