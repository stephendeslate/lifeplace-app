# Cloudflare Pages SPA fallback
# Pre-rendered routes (/, /about, etc.) have their own index.html files.
# All other requests serve the SPA shell for client-side routing.
/*    /__spa-fallback.html   200
